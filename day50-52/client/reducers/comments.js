function comments(state = [], action) {
  const i = action.index
  switch (action.type) {
    case 'ADD_COMMENT':
      console.log('ss')
    default:
      return state
  }
}

export default comments;
